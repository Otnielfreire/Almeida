Algoritmo "Somar_Números"

Var
    N, soma, contador: inteiro
    
Inicio
    escreva("Digite um valor para N: ")
    Leia(N)
contador <- 1
soma <- 0
    Enquanto contador <= N faca
             soma <- soma + contador
             contador <- contador + 1
    FimEnquanto
    Escreva("A soma dos números de 1 até ", N, " é: ", soma)


Fimalgoritmo